version: '3'

services:
  nginx:
    build: .
    ports:
      - "443:443"
    depends_on:
      - my_app

  my_app:
    build: ..
    environment:
    - SOCIAL_SERVER_ADDRESS=${SOCIAL_SERVER_ADDRESS}
